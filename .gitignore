# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# PlatformIO / Arduino
.pio/
.pioenvs/
.piolibdeps/
build/

# ESP-IDF: sdkconfig is generated from sdkconfig.defaults, which IS tracked.
# Committing it would pin another developer to this machine's board settings.
sdkconfig
sdkconfig.old
dependencies.lock
managed_components/

# Generated framebuffers / previews (regenerate, don't commit)
*.bin
out/
preview/

# Environment / secrets
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Private working material — NOT for publication.
# Full implementation detail, the research edge, and how the open problems were
# actually solved. Kept out of the public repo deliberately; see private/README.
private/
